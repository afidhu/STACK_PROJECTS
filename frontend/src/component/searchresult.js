import React from 'react';

const Searchresult = (props) => {
    const all=props.allmyydata
    console.log(all.name)
    return (
        <div>
     
        </div>
    );
}

export default Searchresult;
